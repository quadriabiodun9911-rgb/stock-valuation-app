This branch prepares a staged upgrade of `jest-expo` to a newer compatible version.

Plan:

- Update `jest-expo` in `mobile/package.json`.
- Run and fix unit tests/snapshots in `mobile`.
- Iterate until CI passes, then open follow-up PR with actual code changes.

Note: This file is a placeholder to create the PR and trigger CI; implementation commits will follow.
